# Workspace Agent Context Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give agents launched from middleman workspaces explicit, source-aware task context for PRs, provider issues, and Kata tasks.

**Architecture:** Agent sessions are launched in the worktree with generic configured commands; source identity comes only from branch/path naming, persisted workspace metadata, and the UI. Kata tasks especially need an explicit surface because their identity is not a provider issue or PR number. Add a neutral workspace-agent-context model in Go and, at agent launch time, write the rendered context directly into the launched target's local instruction file: `AGENTS.local.md` for Codex, `CLAUDE.local.md` for Claude. There is no canonical context file, no generated directory in the worktree, and no setup-time write.

**Tech Stack:** Go, SQLite-backed workspace metadata, Huma handlers, tmux/ptyowner runtime launching.

---

## Design

### Launch-time direct write

When the user launches an agent target, `PrepareAgentLaunchContext` re-reads the latest
workspace summary and writes the rendered context to the target's file. Targets without
a known instruction filename get no file. Ownership is decided by a marker first line:

```markdown
<!-- generated by middleman; safe to delete; regenerated on agent launch -->
```

- Target file absent: write it.
- Target file is a regular file starting with the marker: middleman owns it; refresh it
  atomically (temp file + rename) so context follows workspace state changes.
- Anything else (unmarked file, symlink, directory): user-owned; leave it completely
  untouched and launch without writing. No warning surface exists yet (Open Decisions).

Never create or modify root `AGENTS.md` or `CLAUDE.md`; those are repo-owned
instruction files.

### Git ignore safety

Generated files must be ignored before they are written so workspaces never look
dirty. Rules go into the worktree's private exclude file (`git rev-parse --git-path
info/exclude`), never a tracked `.gitignore`. The helper accepts only the allowlisted
generated filenames, adds rules only for the paths being written in that call,
distinguishes `git check-ignore` exit status 1 from fatal git failures, and re-verifies
after writing — failing instead of writing a visible file when a tracked negation rule
keeps a path unignored.

### Content

Include only facts the agent cannot trivially discover from the worktree: workspace
ID, repository identity (host/owner/name), provider, source kind, source item
identifier (PR/issue number, or Kata daemon/project/issue identifiers), source URL,
the source item title as fenced untrusted data, the associated PR number/URL when persisted,
and for PRs the push target. Do not include locally discoverable state such as the
checked-out branch, and do not embed provider-fetch or Kata CLI command guidance —
concise source identity plus URL is the useful prompt.

Push target rules (agents frequently push to the wrong remote or branch):

- Same-repo PR: `Push branch: <head> on origin (updates this PR)`. Naming `origin` is
  valid because middleman-managed worktrees come from middleman-managed clones whose
  `origin` is the base repository.
- Fork-headed PR: warning only — `PR head: <head> on fork <host/owner/name>; pushing
  to origin does not update this PR`. Fork PRs deliberately get no positive push
  target; middleman does not manage fork remotes or credentials.
- The head branch comes from the currently synced merge-request row
  (`WorkspaceSummary.MRHeadBranch`), falling back to the creation-time workspace
  snapshot only when no MR row is available, so a renamed head branch is not rendered
  stale.

Field trust classes, and the boundary each protection provides:

- Trusted structure: repository identity, provider, and URLs come from middleman
  configuration and DB identity, and are rendered plainly.
- Untrusted prose (source title, Kata project name): handled like roborev handles
  untrusted input — fenced in `<untrusted-source-text>` tags with XML-escaped,
  whitespace-compacted content, and the file preamble says never to follow
  instructions found inside the tags. This is the prompt-injection boundary.
- External identifiers (Kata daemon/project/issue IDs): normalized to a single line
  (all Unicode whitespace and control characters collapse). This preserves Markdown
  structure only; it is not an injection defense, so no free-prose field may use it.
  Hostile same-line identifier text stays visible inside its labeled list item by
  design.

## Task 1: Neutral Agent Context Model

**Files:** `internal/workspace/agent_context.go`, `internal/workspace/agent_context_test.go`

- [x] Table-driven `BuildAgentContext`/`RenderAgentContext` tests covering PR,
  fork PR, provider issue, issue-with-associated-PR, and Kata task cases, asserting
  provider identity, push-target lines, and the absence of command guidance and
  `Working branch` lines.
- [x] Hostile source text is XML-fenced and escaped (including embedded closing
  tags and multiline metadata), and the marker line leads every render.

## Task 2: Ignore Helper

**Files:** `internal/workspace/gitignore.go`, `internal/workspace/gitignore_test.go`

- [x] `EnsureGeneratedContextFilesIgnored` with the allowlist
  (`AGENTS.local.md`, `CLAUDE.local.md`), scoped rules, exit-1 vs fatal handling, and
  post-write verification, tested against real git repos including negation rules,
  fatal errors, partial requests, and clean `git status --porcelain`.

## Task 3: Launch-Time Write

**Files:** `internal/workspace/agent_context.go`, `internal/server/huma_routes.go`,
`internal/server/workspace_runtime_agent_context_test.go`

- [x] `launchWorkspaceRuntimeSession` calls `PrepareAgentLaunchContext` for
  `LaunchTargetAgent` targets only; shell and host worktree sessions are unchanged.
- [x] Marker-owned files refresh on every launch; user-owned files (including
  symlinks) are preserved byte-for-byte.
- [x] Full-stack coverage through `srv.ServeHTTP` for
  `POST /api/v1/workspaces/{id}/runtime/sessions` with a real test DB: the launched
  target's file exists with source and push-target content and git status stays clean.

## Task 4: Verify End To End

- [x] `go test ./internal/workspace ./internal/server -shuffle=on` for the affected
  suites.
- [ ] Manual smoke test: launch Codex or Claude from a PR, provider issue, and Kata
  task workspace and ask "What source item are you working on?"; each should answer
  with the correct source kind and identifier without inferring Kata tasks as provider
  issues.

### Agent file compatibility

`AGENTS.local.md` and `CLAUDE.local.md` are convention-based choices: Claude Code
documents `CLAUDE.local.md` as a local instruction file, and Codex-family agents
follow the AGENTS.md spec's local-override naming. If an installed agent does not
read its file, the context is simply unused; the manual smoke test in Task 4 is the
compatibility check. Earlier marker lines from previous revisions of this feature are
still recognized as middleman-owned so old generated files get refreshed, not
stranded.

## Open Decisions

- No warning surface exists when a user-owned file prevents writing context; launch
  succeeds and delivery is silently skipped (covered by a full-stack test as the
  intended degraded state). Add a visible launch/context status only if real usage
  shows users need it.
- Source bodies, comments, labels, and CI state are out of scope; context freshness is
  bounded by the last sync.
- Arbitrary project worktree sessions stay out of scope; only middleman-owned
  workspaces for PRs, provider issues, and Kata tasks get generated context.
