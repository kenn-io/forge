{
  "private": true,
  "license": "Elastic-2.0",
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "check": "vp fmt --check frontend packages/ui --no-error-on-unmatched-pattern --threads=1 && vp lint frontend packages/ui '!frontend/dist/**' '!frontend/test-results/**' '!packages/ui/src/api/generated/**' '!packages/ui/src/api/roborev/generated/**' --no-error-on-unmatched-pattern --threads=1 && (cd frontend && vp exec -- svelte-check --tsconfig ./tsconfig.json --fail-on-warnings) && (cd packages/ui && vp exec -- svelte-check --tsconfig ./tsconfig.json --fail-on-warnings)",
    "demo-workspace": "vp run middleman-frontend#demo-workspace",
    "fmt": "vp fmt frontend packages/ui --no-error-on-unmatched-pattern --threads=1",
    "fmt:check": "vp fmt --check frontend packages/ui --no-error-on-unmatched-pattern --threads=1",
    "test:e2e": "vp run middleman-frontend#test:e2e"
  },
  "devDependencies": {
    "svelte-check": "4.6.0",
    "vite-plus": "0.1.24"
  },
  "devEngines": {
    "packageManager": {
      "name": "bun",
      "onFail": "download"
    },
    "runtime": {
      "name": "node",
      "onFail": "download"
    }
  },
  "packageManager": "bun@1.3.14"
}
