services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "127.0.0.1:18090:18091"
    volumes:
      - .:/app
      - go-mod:/go/pkg/mod
      - go-cache:/go/cache
      - kenn-forge-data:/data
    environment:
      KENN_FORGE_CONFIG_PATH: /app/docker/dev-config.toml
      KENN_FORGE_GITHUB_TOKEN: ${KENN_FORGE_GITHUB_TOKEN}
      GOCACHE: /go/cache
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:18091/healthz >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    command:
      - sh
      - /app/docker/backend-dev-entrypoint.sh

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "127.0.0.1:15173:15173"
    volumes:
      - .:/app
      - /app/frontend/node_modules
    environment:
      KENN_FORGE_API_URL: http://backend:18091
      BUN_INSTALL_FLAGS: --frozen-lockfile
      FRONTEND_DEV_ARGS: --host 0.0.0.0 --port 15173
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:15173/healthz >/dev/null"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 5s
    command:
      - sh
      - /app/docker/frontend-dev-entrypoint.sh

volumes:
  go-mod:
  go-cache:
  kenn-forge-data:
